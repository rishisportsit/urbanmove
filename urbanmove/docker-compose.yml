version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-urbanmove_secret_key}
      - PORT=3000
    volumes:
      - ./data.json:/app/data.json
      - ./logs:/app/logs
    restart: always
